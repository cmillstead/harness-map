__pycache__/
*.py[cod]
.mypy_cache/
.pytest_cache/
.ruff_cache/
.DS_Store

# Implementation plans ONLY. Plans carry local absolute paths and raw process
# detail, and this repo is public — the coding-team lifecycle hooks read plan
# frontmatter from here, so the directory must exist locally while never being
# published.
#
# Everything else under docs/ IS tracked — retros, handoffs, design notes.
# Scrub absolute paths (/Users/<name>) and operator-identifying literals before
# committing one; test_no_absolute_home_literal_in_runtime_modules covers
# runtime modules only, not docs.
docs/plans/
