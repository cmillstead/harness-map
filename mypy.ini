[mypy]
# python_version deliberately omitted: mypy defaults to the interpreter running
# check.sh, which is the same python3 that runs the skill (AMENDMENTS A7).
files = collector.py, render_html.py, serve.py
check_untyped_defs = True
no_implicit_optional = True
warn_unused_ignores = True
warn_redundant_casts = True
