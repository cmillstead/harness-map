# CLAUDE.md — harness-map Upgrades

You are upgrading the `harness-map` skill per the spec set at the path supplied via the
`HARNESS_MAP_SPEC_DIR` environment variable (referenced below as `SPEC_DIR`; part-files
under `SPEC_DIR/spec/`). The spec set is maintained privately and is not distributed with
this repo. This file is binding.
When code you find conflicts with this file or the spec, the code is wrong — fix it, don't imitate it. **Exception:** where live source contradicts a *fact this spec cites* (a line number moved, a helper renamed, behavior changed by an interim commit), reality wins — record a one-line amendment in `SPEC_DIR/spec/AMENDMENTS.md` per SPEC_1 §4 and proceed per the spec's intent.

## Orientation (read once)

harness-map is a production-grade, read-only inventory of the Claude Code harness: `collector.py` (deterministic stdlib signals → schema-pinned JSON) → model synthesis (judgments → report + synthesis JSON) → `render_html.py` (offline HTML) → `serve.py` (hardened loopback dashboard). It must never write inside the harness it scans, must always emit a valid JSON envelope, and ships portability tests that must never weaken. You are executing a staged upgrade: corrections first (phantom-ref fix, typing, hygiene), then staleness/trends, loop closure, drag economics, and monitoring/generalization. The spec set was verified line-by-line against source on 2026-07-18 (`SPEC_DIR/spec/AMENDMENTS.md`, "Source verification record") — build from it directly; do not re-verify wholesale.

## How to load context (do this, not more)

1. Always: this file + `SPEC_DIR/MASTER_SPEC.md` (index) + `SPEC_DIR/MILESTONE.md` + `SPEC_DIR/spec/AMENDMENTS.md` (skim).
2. Then ONLY the part-files the index's reading list names for your milestone.
3. Update `SPEC_DIR/MILESTONE.md` when an item or milestone completes.

## Binding rules (violations are bugs, not style choices)

1. **Authority order on conflict:** SPEC_1 §2 hard invariants > recorded amendments > this file > MASTER_SPEC conventions > SPEC_2/SPEC_3 > SPEC_4–7 > the audit > existing code style > your judgment.
2. **[DECISION] markers are final.** Do not reopen or work around them. If one blocks you, stop and ask the operator.
3. **Drift protocol.** Source verification is complete; there is no verify-then-build gate. If live source contradicts a spec-cited fact, record a one-line amendment and proceed per the spec's intent; halt only if the contradiction touches a SPEC_1 §2 invariant or makes a [DECISION] unimplementable (SPEC_1 §3). **Symbol names are authoritative; line numbers are advisory.** The tree has drifted far past the 2026-07-18 snapshot (AMENDMENTS A12; current sizes and the command to measure them live under "Key numbers" below — one home, not two); at each milestone start, re-locate every cited symbol by name with Grep — never trust snapshot line numbers.
4. **Read-only posture.** No new write paths, anywhere, ever. The collector writes only a validated outside-root `--out`; the renderer only via `write_html_safely`. `--check` writes nothing.
5. **Envelope rule.** Every new collector field exists (null/empty) in `_empty_document`; `main()` must still emit valid JSON on any crash.
6. **Signals vs judgments.** Collector code never classifies, condemns, or verdicts. Deterministic renderer arithmetic (joins, scores) is a data operation; verdict words stay the model's. Drag verdicts cap at `probation`; schema.md's D8 `retire safely` cap is unconditional — friction telemetry never lifts it (AMENDMENTS A4).
7. **Never edit an existing test assertion.** Additions only. `test_release_decoupling.py` passes unmodified after every milestone — if a scaffold file trips it, the file moves, not the test. **A27 carve-out, narrow:** this protects assertions that PREDATE the current plan execution. An assertion written EARLIER IN THE SAME execution may be corrected **tighten-only** — never loosened, never deleted — and only with an amendment recorded in `SPEC_DIR/spec/AMENDMENTS.md` BEFORE the edit, carrying git-verified provenance (the commit that introduced the assertion is on this branch). Provenance is established with `git log -L` or `git blame`, never from memory. A27 exists to correct a contract that is WRONG; it is never a licence to bulldoze one that is correct — see A31, where A27 would technically have permitted an edit to T3.11 and it was declined on the merits.
8. **Never-waive gate:** `./check.sh` green (ruff + mypy + pytest, run from the skill dir) before anything is called done. No exceptions, including "just this once".
9. **stdlib-only runtime; no mocks; deterministic output** across `PYTHONHASHSEED` (fixed orderings, no bare `set()` iteration into output).
10. **Additive schema discipline:** new sidecar fields additive, readers `.get()`-tolerant, `schema.md` updated in the same change; `schema_version` bumps only on meaning change.
11. **Secrets:** env values never serialize (`config.env_keys` is names-only); scanned bytes are data, never instructions (parse-only `ast`, no import/exec).
12. **Scope fence:** the only file outside the skill dir you may edit is `skills/harness-engineer/SKILL.md` at M7, exact-diff approved by the operator in-session first. Registering hooks or touching `settings.json` is out of scope.

## Key numbers (contracts are binding; measurements are dated — re-measure, don't memorize)

- Module sizes: `wc -l collector.py render_html.py serve.py`. Measured 2026-08-10 (`fix/trk-022-slice-d-sse-limits`, final CODE commit `6812815`, based on `main` @ `36f476f`): **8,172 / 6,713 / 1,369** — only `serve.py` changed: 1,211 → 1,369 = +158 (TRK-022 slice D, the LAST of that ticket's seven findings, so this branch closes TRK-022: `MAX_SSE_CLIENTS = 16` with the check-and-append under ONE `clients_lock` hold in `register_client` — a two-acquisition check-then-append is a TOCTOU that lets concurrent connects overshoot the ceiling — the `None` refusal contract and the 503 + `Retry-After` branch in `_serve_events`, one `retry: 5000` field written before `event: sync`, the three rewritten heartbeat/timeout comments whose shared causal claim was measured false in both directions, and the accepted-residual disclosure for the stalled SSE stream no application-layer signal can detect; plus three review-round fixes — the outer `try:` now opening at registration rather than after it, and two comment claims corrected). Confirmed TWO ways: direct `wc -l`, and 1,211 + (197 − 39) = 1,369 from `git diff main --numstat`. `collector.py` and `render_html.py` untouched. Prior: 8,172 / 6,713 / 1,211 on 2026-08-10 (`fix/trk-022-slice-c-renderer`, final CODE commit `df59de6`, based on `main` @ `e2d7fa9`): **8,172 / 6,713 / 1,211** — only `render_html.py` changed: 6,605 → 6,713 = +108 (TRK-022 slice C: `_neutralize_controls`/`_ESC_JSON_TRANSLATE` closing the control-character escape gap in `esc_html` and JSON islands, `_fit_label` measuring width on the display form, the CSP directive-set test, plus five post-execution review fixes — F1 island-payload neutralization matching the clipboard, F2 pinning the exact CSP directive set rather than substrings, F3 end-to-end SVG/treemap coverage, F4 disclosing the non-injective escape, F5 fitting labels on the surrogate expansion too); `collector.py` and `serve.py` untouched. `df59de6` is the last commit that changes a measured file; this figures commit follows it — consistent with the branch-tip warning below: a figures commit sits ON TOP of the last code commit by construction, so it can never name the tip it is written at. Prior: 8,172 / 6,605 / 1,211 on 2026-08-08 (`fix/trk-022-slice-b-watched-set`, final CODE commit `1a247f9`, based on `main` @ `6f296ff`) — `collector.py` grew by 183 lines net (TRK-022 slice B: `iter_input_paths` now watches absolute outside-root hook targets at the operator tier, and a new bounded-retry read helper `_read_text_stable` is wired at both `_read_text` and `parse_settings`' direct read; plus eight post-execution review fixes, of which four touch `collector.py` — F1 the NUL-path skip the removed containment filter used to provide incidentally, F2 and F6 docstring corrections, and F5 the retry-read OSError guard); `render_html.py` and `serve.py` untouched. **`fcf7116` is NOT the branch tip** and the earlier text calling it one was wrong: the figures commit sits ON TOP of the last code commit by construction, so a figure entry can never name the tip it is written at. `1a247f9` is the last commit that changes a measured file; this figures commit follows it. That is also why this entry was rewritten TWICE on one branch — a first figures commit at `36ce6c4` went stale the moment a second review round landed. Do not write the figures until every review round is closed. Prior: 7,989 / 6,605 / 1,211 on 2026-08-07 (`fix/trk-022-watcher-snapshot-blind-spots`, final branch tip `6e40614`, based on `main` @ `8d86481`) — only `serve.py` changed, 1,084 → 1,211 = +127 (TRK-022: the `_path_value` symlink-retarget widening — element 4 now `(realpath-target, target-mtime_ns)` instead of a bare target-mtime — and the `_classify_stream_sweep` same-size telemetry-rotation detection via a parallel `stream_inodes` dict, plus a review-fix commit correcting a mixed-seeding bug that could permanently disable the rotation check for a stream); `collector.py` and `render_html.py` untouched. Prior: 7,989 / 6,605 / 1,084 on 2026-08-07 (`fix/trk-026-duplication-corpus-containment`, final branch tip `612c5b7`, based on `main` @ `da6621c`) — `collector.py` grew by 52 lines (TRK-026: gated `_project_tier_duplication_corpus`'s surface-directory enumeration through `_probe_is_dir` → `_project_tier_gate` before enumerating, matching the shape the sibling hygiene corpus already shipped, plus the review-fix commits — distinct `blind_spots` text on the glob-failure branch, corrected stale A64 known-gap comments, and an oracle test pinning the site-3 per-skill-directory gate); `render_html.py` and `serve.py` untouched. Prior: 7,937 / 6,605 / 1,084 on 2026-08-07 (`feat/trk-023-project-tier-hygiene`, final branch tip `a4ad517`, based on `main` @ `ab17a32`): `collector.py` grew by 314 lines and `render_html.py` by 19 (TRK-023 slice A: `_project_tier_hygiene_corpus`, the project-tier wiring for `collect_promotion_candidates`, the `hygiene_tiers` scope field, the two-metric taint-prefix addition, the rewritten compose disclosure, and four post-execution Codex review fixes — F1/F3 the containment gate and probe, F2 the partial-scan taint, F6 the documentation seam); `serve.py` untouched. Prior: 7,879 / 6,605 / 1,084 on 2026-08-07 (`feat/trk-023-project-tier-hygiene` @ `29e0015`, T8 close, before the four review-fix commits landed) — this reading went stale before the ticket finished, the same trap recorded in the lesson below: measure at the FINAL tip, not an intermediate commit. Prior: 7,623 / 6,586 / 1,084 on 2026-08-07 (`fix/trk-024-live-verify-merge-table`, based on `main` @ `7b88b9f`) — `collector.py` grew by 6 lines (TRK-024 retired the live-verify deferral prose at both `_SURFACE_MERGE` comment sites, comment-only, no behavior change); this ticket touched neither `render_html.py` nor `serve.py`. **Staleness note on the prior entry:** its 7,615 / +72 were correctly measured **for the mid-branch commit it named** (`6508262`, TRK-056 T3) — git-verified, both figures hold at that hash. But T8 (`8f9c6ca`) later added 2 more lines to the same file, so the mid-branch figure went stale before the ticket finished; `main` @ `7b88b9f` is **7,617**, and TRK-056's completed delta is **+74**, not +72. Lesson: measure module sizes at the FINAL tip of a branch, not an intermediate commit — a figure labelled with a mid-branch hash goes stale the moment a later commit touches that file. TRK-056's commit hashes and merged PR are unaffected. Prior: 7,543 / 6,586 / 1,084 on 2026-08-06 @ `6119d5c` (`fix/trk-082-glob-listability`) — `collector.py` grew by 153 lines (TRK-082 glob listability); TRK-050 before it added 220; TRK-050 touches neither `render_html.py` nor `serve.py`. Prior: 7,170 / 6,586 / 1,084 on 2026-08-06 @ `db540d7` (`fix/trk-049-pathological-fixtures`) — the growth over the S6c figure below is TRK-051's (merged as PR #16, `44ac6d7`); TRK-049 touched neither `collector.py` nor `render_html.py` (7,072 / 6,576 / 1,084 on 2026-08-06 @ `abfaef9`, S6c close; 6,985 / 5,311 / 1,084 on 2026-08-05 @ `92e3555`; 6,347 / 5,204 / 1,084 on 2026-08-04 @ `c27b39d`; 1,784 / 2,896 / 832 at the 2026-07-18 snapshot; AMENDMENTS A12). Advisory only — **re-locate every cited symbol by name with Grep; never trust a line number in this file or in the spec.**
- SKILL.md hard cap **200** lines, working cap **195**, budgeted additions **≤26** (SPEC_3 §5) — contracts, changed only by spec amendment. Size: `wc -l SKILL.md`. Measured 2026-08-10 (`fix/trk-022-slice-d-sse-limits`, final CODE commit `6812815`): **112**, unchanged — this slice edited no SKILL.md line, and the 16-client cap is deliberately not documented there (a gap, not drift: no shipped sentence became false). Prior: 112 on 2026-08-10 (`fix/trk-022-slice-c-renderer`, final CODE commit `df59de6`) — that slice edited no SKILL.md line either. Prior: 112 on 2026-08-07 (`feat/trk-023-project-tier-hygiene`, after T7's compose-disclosure rewrite) — T7 edited existing lines (1 insertion, 1 deletion) rather than adding any. Prior: 112 on 2026-08-06 (110 on 2026-08-05; 80 at the 2026-07-18 snapshot, 99 at S1 completion, then 101 both pre-S6a and pre-M11 — `git show 44e1df4^:SKILL.md | wc -l`; 106 pre-M10; AMENDMENTS A12). 112 is inside every cap on either reading of the ≤26 budget's base; if that distinction ever becomes load-bearing, resolve it against SPEC_3 §5, not against this line.
- Description rewrite target **≤70** words (from the verified 88), all 4 positive + 2 negative triggers preserved (SPEC_3 §4).
- Test baseline: `python3 -m pytest -q` from the skill dir. Measured 2026-08-10 (`fix/trk-022-slice-d-sse-limits`, final CODE commit `6812815`, based on `main` @ `36f476f`, macOS): **1339 passed / 4 skipped** in a plain environment, `CHECK GREEN`, ruff and mypy clean (TRK-022 slice D added 11 tests over the 1328/4 slice C baseline — 9 planned plus 2 from the two post-execution review rounds; `git diff main --numstat` shows `serve.py 197/39`, `tests/test_serve.py 522/0`, **zero test deletions**. The atomicity pin took THREE attempts and the first two shipped VACUOUS: measured against a check-outside-the-lock mutation of `register_client`, both `test_register_client_checks_and_appends_under_one_lock_hold` and `test_register_client_appends_inside_the_hold_that_checked_the_ceiling` PASSED while only the third, `test_register_client_reads_the_length_and_appends_under_the_SAME_hold`, FAILED with the op log `[('len', None), ('append', 1)]` — the working design instruments the LIST, not the lock, recording which hold was in effect at each `__len__` and each `append` and asserting they are the same non-None hold). The full-suite figure with `HARNESS_MAP_REAL_ROOT` set is **1340 passed / 3 skipped** (measured 2026-08-10 at this same tip, `HARNESS_MAP_REAL_ROOT=$HOME/.claude python3 -m pytest -q`, 210.11s — measured, not derived; the 4th skip `test_real_root_acceptance_no_crash_envelope_and_hooks_classified` is what becomes a pass). Prior: 1328 passed / 4 skipped, measured 2026-08-10 (`fix/trk-022-slice-c-renderer`, final CODE commit `df59de6`, based on `main` @ `e2d7fa9`, macOS) in a plain environment (TRK-022 slice C added 15 tests over the 1313/4 TRK-022 slice B baseline — `git diff main --numstat` shows `render_html.py 115/7`, `tests/test_render_html.py 319/0`, zero test deletions; slice C fixed two renderer findings — Finding 5 the control-character escape gap in `esc_html`/JSON islands and Finding 7 the untested CSP policy — plus five post-execution review fixes: F1 the copy-preview/clipboard mismatch, F2 the CSP substring-pinning gap, F3 missing end-to-end SVG/treemap coverage, F4 the undisclosed non-injective escape, F5 `_fit_label` not neutralizing lone surrogates before `esc_html`'s 1→6 expansion). The full-suite figure with `HARNESS_MAP_REAL_ROOT` set is **1329 passed / 3 skipped** (measured 2026-08-10, `HARNESS_MAP_REAL_ROOT=$HOME/.claude`). Prior: 1313 passed / 4 skipped, measured 2026-08-08 (`fix/trk-022-slice-b-watched-set`, final CODE commit `1a247f9`, based on `main` @ `6f296ff`, macOS) in a plain environment (TRK-022 slice B added 17 tests over the 1296/4 TRK-022 slice A baseline — 13 in T1–T5 plus 4 across the eight post-execution review fixes: F1 +1, F5 +2, F7 +1, while F3 and F8 ADD an assertion to an existing test and F2/F6 are docstring-only — all additive, zero deletions in either test file: `git diff main --numstat` shows `collector.py 213/30`, `tests/test_collector.py 434/0`, `tests/test_serve.py 26/0`; slice B fixed two measured watched-set defects: `iter_input_paths` dropped absolute outside-root hook targets so no watcher saw them change, and both `_read_text` and `parse_settings`' direct read could return a torn read of a file being rewritten. The review then found TWO defects slice B itself introduced, both of the same inverse-bug shape TRK-050's review caught — F1, the removed containment filter having ALSO been an incidental NUL-path sanitizer, and F5, the in-loop retry read having no OSError guard so a healthy attempt-0 read was discarded and reported INACCESSIBLE. F3 re-pinned the stat-failure test by STAT count because its read count did not discriminate against a broken `after == before`, and F8 named the path that moved rather than asserting only that the snapshot moved). The full-suite figure with `HARNESS_MAP_REAL_ROOT` set is **1314 passed / 3 skipped** (measured 2026-08-08, `HARNESS_MAP_REAL_ROOT=$HOME/.claude`). Prior: 1296 passed / 4 skipped, measured 2026-08-07 (`fix/trk-022-watcher-snapshot-blind-spots`, final branch tip `6e40614`, macOS) in a plain environment (TRK-022 added 18 tests over the 1278/4 TRK-026 baseline, all additive, zero deletions — `git diff main --numstat` shows `serve.py 151/24`, `tests/test_serve.py 377/0`; TRK-022 slice A fixed two measured watcher blind spots: `_path_value` could not see a metadata-identical symlink retarget, and `_classify_stream_sweep` could not see a same-size telemetry rotation) — the full-suite figure with `HARNESS_MAP_REAL_ROOT` set was **1297 passed / 3 skipped** (measured 2026-08-07, `HARNESS_MAP_REAL_ROOT=$HOME/.claude`). Prior: 1278 passed / 4 skipped, measured 2026-08-07 (`fix/trk-026-duplication-corpus-containment`, final branch tip `612c5b7`, macOS) in a plain environment (TRK-026 added 5 tests over the 1273/4 TRK-023 baseline, all additive, zero deletions — `git diff main --numstat` shows `collector.py 94/42`, `tests/test_collector.py 132/0`). The 4th skip is still `test_real_root_acceptance_no_crash_envelope_and_hooks_classified`, gated on **`HARNESS_MAP_REAL_ROOT`** — the full-suite figure with the var set is **1279 passed / 3 skipped** (measured 2026-08-07, `HARNESS_MAP_REAL_ROOT=$HOME/.claude`). Prior: 1273 passed / 4 skipped, measured 2026-08-07 (`feat/trk-023-project-tier-hygiene`, final branch tip `a4ad517`, macOS) in a plain environment; **1277 items** (TRK-023 slice A added 44 tests over the 1229/4/1233 TRK-056 baseline, all additive, zero deletions in either test file — T2 `_project_tier_hygiene_corpus` +15, T4 the `collect_promotion_candidates` project-tier wiring +4, T5 `hygiene_tiers` scope wiring +10, T6 the two-metric taint-prefix addition +5, T7 the compose-disclosure rewrite +2, T8 schema.md docs +0, then four post-execution Codex review fixes — F1 +4, F2 +2, F6 +2; F1 also strengthened one pre-existing test with an added assertion, which adds no test count) — the full-suite figure with the var set was **1274 passed / 3 skipped** (measured 2026-08-07, `HARNESS_MAP_REAL_ROOT=$HOME/.claude`). Prior: 1265 passed / 4 skipped, 1269 items, measured 2026-08-07 (`feat/trk-023-project-tier-hygiene` @ `29e0015`, T8 close, before the four review-fix commits landed) — this reading went stale before the ticket finished. Prior: 1229 passed / 4 skipped, 1233 items, measured 2026-08-06 (`fix/trk-056-quoted-control-operators` @ `6508262`, macOS) — TRK-056 added 3 tests, all in `tests/test_collector.py`, over the 1226/4/1230 TRK-082 baseline; TRK-082 added 37 tests: 6 in T1, 16 in T2, 11 in T3, 4 in T4, over the 1189/4/1193 TRK-050 baseline; TRK-050 added 22 before it; the full-suite figure with `HARNESS_MAP_REAL_ROOT` set was 1230 passed / 3 skipped (measured 2026-08-06 at `6508262`). `check.sh` still prints a `notice:` line when `HARNESS_MAP_REAL_ROOT` is unset, disclosing that the real-input acceptance check did not run — visible on every plain green run. The `HARNESS_MAP_REAL_SAMPLE` figure was **NOT re-measured at this baseline** — fixture generation remains unavailable — so by the documented 2-test delta (the two smoke tests), re-based onto the new 1229/4 plain baseline, it should read 1231 passed / 2 skipped; treat that as DERIVED, and re-measure before relying on it. Its last measured value was 966 passed / 1 skipped against a 964/3 plain baseline (353 at the 2026-07-18 snapshot; 520 entering S1 and 523 at its completion; 765 at S6a, 851 at S6b, 952 at TRK-041, 960 at TRK-025, 964 at TRK-027+TRK-044, 979 at TRK-047/M10, 981 at the TRK-047 drift pins, 1005 at TRK-051, 1143 at S6c, 1167 at TRK-049, 1189 at TRK-050, 1226 at TRK-082, 1229 at TRK-056 — AMENDMENTS A10, A28, A48, A50, A51–A57). Count only goes up; existing tests untouched.
- **Two measuring traps.** (1) A PIPED `--collect-only` returns a wrapper line reading `Pytest: No tests collected`, which is not the real result — redirect to a file and read the file; a piped plain `python3 -m pytest -q` is unreliable too, producing a spurious `ModuleNotFoundError: No module named 'collector'` that does not occur when redirected to a file — always redirect. (2) `HARNESS_MAP_REAL_SAMPLE` takes a **path to a sidecar named exactly `harness-map-2026-07-15.json`** — not a boolean, and not merely any name matching `find_sidecars`' `harness-map-YYYY-MM-DD.json` regex: both smoke tests render with `--date 2026-07-15`, so the basename must carry that exact date. **Three outcomes; the SKIP case last measured at the TRK-049 baseline (`fix/trk-049-pathological-fixtures` @ `db540d7`), all three now re-based by the documented 2-test delta onto the current 1229/4 plain baseline and treated as DERIVED.** Unset, or a path that does not exist ⇒ both tests **SKIP** (1229 passed / 4 skipped). A file that **exists** under any other name ⇒ both tests **RUN and FAIL** (2 failed / 1229 passed / 2 skipped — derived, see the baseline bullet above) — the skip is gated on `REAL_SAMPLE.is_file()` alone, so an existing-but-misnamed fixture does NOT skip. The error distinguishes the two misses: a basename outside the `harness-map-YYYY-MM-DD.json` regex gives `fatal: zero sidecars found`, whereas a regex-valid but wrong-date name (e.g. `harness-map-2026-07-14.json`) gives `fatal: no sidecar found for date 2026-07-15`. The exact name ⇒ both **RUN and PASS** (1231 passed / 2 skipped — derived, see the baseline bullet above). That middle case is the trap: it is a fixture error, never a renderer regression.
- Test placement (AMENDMENTS A8): renderer features → `test_render_html.py`; collector features → `test_collector.py`. M11 was the single milestone permitted a new test file and has used it — `tests/test_profiles.py` exists; no further new test files.
- Phantom fix: **DONE in S1.M0** — candidate order `root/norm` then `root/<source-dir>/norm`, `_safe_exists` tri-state preserved (SPEC_3 §1; AMENDMENTS A11). Find it by symbol: `check_phantom_refs`, whose two-candidate probe sits just below `src_dir = Path(rel_path).parent`. Line numbers are advisory and have moved twice — `:1166` at snapshot, `:2242` at S1.M0, `:4977` on 2026-08-04.
- Drag: `est_tokens × (1 + friction_events_30d)`, 30-day window computed POST-join, undated records excluded (SPEC_6 §1). Cost: median **events/day** (never "turns/day") over **14** days, min **3** active days (SPEC_6 §2).
- Trend sparklines: display window **N=10** points, appear at ≥3 MEASURED points per series (a crashed run or a missing headline key contributes no point; sidecar count alone is not the gate); loader already exists — visualization only (SPEC_4 §3, AMENDMENTS A2).
- `--check`: exit **0/1/2**; `CHECK_BANDS` = report-template thresholds **5,000 / 12,000** tokens (NOT the renderer's `GAUGE_BANDS` 6,000/15,000 — two homes exist, AMENDMENTS A3); wall-time budget **≤5s** (SPEC_7 §1).
- Kill signals: >2 staleness false positives per real run; >50 mypy errors; >10 type-ignores; 1 edited existing assertion; <10 dated joinable telemetry records at M8 (RISK_REGISTER).
- **When a measured figure above is wrong, correct it in the same change.** `~/.claude/hooks/write-guard.py` (NOT a repo-local hook) is DORMANT while no plan under `docs/plans/` carries `status: in-progress` — with none active the edit is simply allowed. When one IS active it must declare `instruction_files: CLAUDE.md`, or the edit is blocked; routing through the Agent tool alone does NOT satisfy it. `WRITE_GUARD_ALLOW_INSTRUCTION_EDIT=1` does bypass that gate and is the documented recovery for a wedged or ambiguous plan state — but it is an EMERGENCY route only, never the routine one, because it disarms the gate for every instruction file for the whole session.

## Workflow expectations

- `./check.sh` green before claiming any task complete. New behavior needs tests in the same change, real-fixture style (extend `conftest.py::fake_harness` and the `run_collector`/`run_render` subprocess drivers — no mocks).
- Spec constants encoded in tests carry: `# Changing this value requires a spec change (SPEC_N §M).`
- No pre-created stubs: each milestone adds its functions with tests; contracts live in the spec (SPEC_2 §5).
- Fixture policy: fixtures are real temp trees/repos/streams built in-test; nothing golden is regenerated without a spec section saying how.
- No new top-level files in the skill dir beyond the ledger in SPEC_2 §4; no new dependencies, period.
- Commit style: `harness-map S<stage>.M<n>: <imperative summary>`.
- A collector detector change is not done until it has been run once against a real harness root and the expected numbers named in the change. A green suite over `fake_harness` does not certify a detector — the fixture corpus is systematically gentler than the input the tool is built to read. `HARNESS_MAP_REAL_ROOT=<path> python3 -m pytest -q` runs that check. **It is deliberately a discipline and not a gate — ruled 2026-08-10 (TRK-057), do not reopen without new evidence.** Promoting it was considered and declined: the fixtured shapes are hand-picked from known failures, so a third unanticipated real-harness shape stays invisible to `check.sh` either way, which means a gate buys ceremony rather than coverage — while auto-detecting a root inside `check.sh` would make the default suite non-hermetic (per-machine results), and a separate opt-in script is the same discipline problem one layer up. Two shipped mitigations are what the ruling relies on, and neither may be weakened: a SET-but-invalid `HARNESS_MAP_REAL_ROOT` FAILS rather than skips, so the rule cannot be satisfied by a typo; and `check.sh` prints an explicit `notice:` line on every plain green run when the check did not run, so silence is not indistinguishable from success.

## Current state

**`SPEC_DIR/MILESTONE.md` is authoritative — read it, and prefer it over this summary, which drifts.** As of 2026-08-06: Stages 1, 2, 6a and 6b have shipped, and the scaffold (`check.sh`, `mypy.ini`) has existed since S1.M0 on 2026-07-24 — source verification completed 2026-07-18. TRK-019 + TRK-020 merged 2026-08-02 (PR #5, `06c6f2a`) and TRK-018 / M11 layout profiles merged 2026-08-04 (PR #6, `c27b39d`) — both dates committer-local per `git log %ci`, and GitHub's UTC `mergedAt` reads one day later for each. **M10 (`--check`, SPEC_7 §1) has SHIPPED** (PR #10, `5ef0572`, plus drift pins in PR #11, `80ef33b`), so **Stage 5 is CLOSED** (AMENDMENTS A49). **TRK-051** then fixed six defects a Codex review found in the shipped gate — F1/F2 (root pre-flight + read-before-write), F3 (profile-rejection envelope unmeasured, both homes), F6 (exit 2 on a non-comparable prior; non-list `civc` guard), F7 (per-metric definition-version skip, AMENDMENTS A50) and F9 (these figures) — with F4, F5 and F8 deferred by explicit operator decision. **TRK-021 (UI polish) SHIPPED 2026-08-05** (PR #13, merge `92e3555`): all five operator findings fixed in `render_html.py` + tests (+11 tests), plus one spec-audit fix (SVG-fallback pin) and one Codex P2 fix (pressed expand-all contrast, now WCAG-pinned against both themes) — so **S7 is CLOSED**. **S6c (TRK-040, trend verdicts, SPEC_6 §6.5a–6.8) SHIPPED 2026-08-06** (`feat/trk-040-s6c-trend-verdicts` @ `abfaef9`, all 14 planned tasks complete): `collection_scope` + per-metric quality state, the derived trend key table, the seven-value verdict enum and classifier, the three-axis (scope/quality/denominator) comparability engine, the `trend_basis` digest recompute with stale-prose suppression, the rendered verdict surface across both trend tables, the A52 blackout disclosure, sparkline presentation and discoverability fixes, plus the `schema.md`/`synthesis-template.json`/SKILL.md updates the deliverable list requires — gate green (1143 passed / 3 skipped), determinism and leak scan clean, **merged as PR #15, merge commit `46376a3`, 2026-08-06** — verified by ancestry (`git branch -r --merged main` lists the branch), not from a memo (AMENDMENTS A51–A57). **TRK-050 slice A+B (comprehension abort) MERGED as PR #18, merge commit `8ea7ae5`, 2026-08-06**: fixed the comprehension-abort defect at **all 12 sites** — measured, one unreadable child aborted the whole generator and lost every sibling while the handler recorded nothing. Split into a listing-level record and a per-child record, with entries sorted before the per-child loop so the record order stays deterministic. Two functions gained an optional `errors` sink following the existing `_skill_has_test_asset(errors=None)` precedent, defaulting to `None` so every existing call site is unchanged. **Category 5 is CLOSED as empty** — both `scandir` sites already recorded correctly. The **20 glob sites are explicitly NOT touched**: measured, `Path.glob()` returns `[]` and raises nothing on a locked directory, so their handlers are dead code for that case — ruled on in spec **AMENDMENTS A59**, carried by **TRK-082**. Verified on the real harness: output is a **no-op vs `main`** — all 10 headline values identical, `errors`/`inaccessible` empty on both, `blind_spots` 9 on both, correct for a defensive fix that speaks only when something is genuinely unreadable. Determinism re-checked: byte-identical across two `PYTHONHASHSEED` values, the only differing field being the wall-clock `generated_at`. Two new tickets filed: **TRK-082** (glob sites) and **TRK-085** (`--check` exits 0 reporting "no baseline" when it could not READ the sidecar directory — needs an exit-code ruling first). A fifth commit then fixed five review findings, the most important being that **absent optional directories were being recorded as collection errors** — the inverse of the bug being fixed, which would have shown 2 problems that do not exist on a healthy harness. Verified in both directions: absent now records 0, present-but-unlistable still records 1, so the fix did not buy silence. The branch shipped **+22 tests with zero test deletions**, and **TRK-086** (wiring the new `errors` sink) is now unblocked because its stated blocker — that absent-directory fix — shipped in this PR. **TRK-082 (glob listability) MERGED as PR #20, merge commit `26ea226`, 2026-08-06** (branch `fix/trk-082-glob-listability`, 5 commits: T1 helper, T2 15 sites, T3 4 pattern-loop sites + a determinism fix, T4 the disclosure) — verified by ancestry (`git log` shows the PR #20 merge commit in this branch's history), not from a memo. The defect: `Path.glob()` returns `[]` and raises nothing on a locked directory, so an unreadable surface read as an empty one — contradicting the skill's own **inaccessible ≠ clean** invariant. Re-measured across all three cases (locked-with-a-real-match, genuinely empty, absent), glob returns `[]` for all three; an `os.scandir` probe separates them. The fix: one helper, `_disclose_unlistable_glob`, probing **only on the empty path**, wired at **19 in-scope sites**. Per **AMENDMENTS A60**, the fixable/disclosed line is a property of the **pattern**, not the call site — a pattern is probeable iff its directory portion carries no wildcard — so the helper self-selects and the 4 pattern-loop sites needed no hand-classification. Over the 18 distinct reachable patterns: **11 probeable, 7 not**. The 7 not-probeable patterns are exactly the `skills/*/…` family and get a **standing, unconditional `blind_spots` entry** rather than a fix, per **AMENDMENTS A59** requirement 4 — the disclosure IS the deliverable for that half. It is unconditional by design: the failure is undetectable at this layer, so a conditional disclosure would be silent precisely when it matters. `_hook_disk_files` was the one site with no sink in scope and gained an optional `errors=None` parameter, the precedent TRK-050 established twice — every existing call site unchanged. `inaccessible` is `list[dict]`, not `list[str]` — the renderer reads `.get("path")` on every entry — so the three sites whose sink is `inaccessible` route through the existing `_append_inaccessible_once` rather than appending a raw string. Verified on the real harness by a same-moment A/B against `main`: **`errors` 0 and `inaccessible` 0 on both**, headline identical, `blind_spots` **9 → 10** (the one new standing entry; every prior entry preserved verbatim and shifted by one). Determinism re-checked: record order byte-identical across four `PYTHONHASHSEED` values after T3's `sorted(set(...))` fix, which was measured producing four different orders before it. The A16 golden literal was regenerated for the new standing entry: structural diff proves **0 entries lost, exactly 1 added, every other key identical**, and both golden assertion functions are **byte-identical**. **TRK-056 (quoted control operators) SHIPPED 2026-08-07** (PR #21, merge commit `7b88b9f`, branch `fix/trk-056-quoted-control-operators`, final commit `8f9c6ca`, 6 commits): T1 (amendment **A62**, committed in the VAULT repo at `bd37f36` — the vault is its own git repo), T2 the quote-aware scanner (`78776f8`), T3 the stale-prose correction (`6508262`), T5 these figures (`b853ca5`), T7 `schema.md`: document the third route into `commands_unparsed` (`f368915`), T8 scope the brace-residual comment to outside-quotes (`8f9c6ca`). The defect: `_has_shell_control_syntax` substring-scanned the RAW command for `$( && || | ; < > { }` and backtick, so an operator sitting **inside quotes** was read as proof a shell was interpreting the command — classifying an opaque external program `no_script` ("fully examined, zero orphan risk") instead of `unparsed` (a disclosed blind spot), inverting the skill's **inaccessible ≠ clean** invariant. The fix: one left-to-right quote-state scan — inside SINGLE quotes nothing counts; inside DOUBLE quotes `$(` and backtick STILL count (they still expand) while the rest do not; a backslash escapes the next character everywhere EXCEPT inside single quotes. A62 **narrows** A45 step 2 — it does not reopen the `[DECISION]`. Deliberately NOT fixed, and recorded as **known residuals of the A45 heuristic** — never "correct", "intentional" or "by design": `&`, newline, `(`/`)`, `${…}`, `$((…))`, bare braces, comment-only and assignment-only input. Rationale is C10 (command-grammar parsing was removed once already after an arms race); narrowing `{`/`}` would additionally break **8 shipped assertions**, which binding rule 7 forbids. Verified on the real harness by a same-moment A/B against `main` with a **positive control** proving the two collectors are genuinely different code (they disagree on `rtk hook "a && z"` — main True, fix False — and agree on the real unquoted `rtk hook claude && echo hi`): **all 7 A/B fields identical** — `hook_commands_examined` 12→12, `hook_commands_total` 12→12, `commands_no_script` 8→8, `commands_unparsed` 0→0, `commands_resolved` 4→4, `commands_total` 12→12, `blind_spots` 10→10 with identical content; `errors` 0 and `inaccessible` 0 on both sides. Zero classification flips is the CORRECT result: every genuine inline-shell command among the 12 real ones carries its operators outside quotes as well. Determinism re-checked: byte-identical across two `PYTHONHASHSEED` values, the only differing field being the wall-clock `generated_at`. `tests/test_collector.py` **+3 tests, zero deletions** — no existing assertion edited. `schema.md` described only 2 of the 3 routes into `commands_unparsed`, omitting the bare-opaque-invocation route (note text `unsupported hook command form: <command>`). That gap PREDATES TRK-056 — verified by direct call, `rtk hook foo` classifies `unparsed` on both `main` and this branch — but A62 routes more commands through it. Verified by running the collector on a purpose-built root: `commands_unparsed` 1 and one `blind_spots` entry reading `unsupported hook command form: rtk hook foo`. The known-residuals comment claimed bare `{`/`}` are scanned "unconditionally, quoted or not". T2 made that false and T8 corrected it. Measured: `rtk hook {foo}` → True, `rtk hook "{foo}"` → False, `rtk hook '{foo}'` → False. Comment only, no behavior change; the residual and its rationale are unchanged. **TRK-024 (merge-table live-verification) SHIPPED 2026-08-07** (PR #22, merge commit `ab17a32`, branch `fix/trk-024-live-verify-merge-table`): the per-surface merge table (`_SURFACE_MERGE`) is live-verified 2026-08-07 against real `claude -p` sessions (CC 2.1.224, macOS) — all six surfaces (skill, command, agent, rule, claude_md, hook) matched the shipped table, including the skill/agent shadow-direction inversion, now measured rather than inferred, not merely documented intent. No code change to the table. Recorded as **AMENDMENTS A63**, merged to the vault repo via PR #23 (`ee818884`). Retired the stale `live-verify deferred 2026-07-22` deferral prose at both `collector.py` `_SURFACE_MERGE` comment sites and at `SKILL.md`'s compose-mode paragraph; comment-only, no behavior change. **TRK-023 slice A (project-tier hygiene for `collect_promotion_candidates`) IN PROGRESS ON BRANCH** `feat/trk-023-project-tier-hygiene` (not yet merged), 6 code commits (T2 `0b1e85e`, T4 `50ff5cf`, T5 `6727557`, T6 `cec6e8b`, T7 `ec7aef1`, T8 `29e0015`) plus T1 (amendment **A64**, committed in the vault repo, merged to the vault via PR #24, merge `59799e0c`) and T11 (real-harness verification, no code commit). Shipped: the containment-gated project-tier corpus (`_project_tier_hygiene_corpus`) and `collect_promotion_candidates` extended to cover the project tier — **one** detector, not two. **`check_phantom_refs` was CUT from slice A on a security-design ground, not deferred for lack of time**: the function decides whether a reference is broken by asking the filesystem whether a file exists, so fed an untrusted root, being an existence oracle is what it does, not a bug in it. Four distinct oracle sites were found in it during review, each surfacing only after the previous one was fixed, and two were created by fixes ordered during review. `collect_promotion_candidates` (`collector.py:5710–5751`) touches the filesystem zero times and drew no security finding across three cross-model review rounds — that asymmetry is the whole reason for the split. Recorded with evidence in amendment **A64** (ruling 1). The v1 compose disclosure is rewritten, not narrowed by deletion, satisfying binding rule 7's protection of `test_compose_hygiene_scans_are_operator_only_and_disclosed` (`tests/test_collector.py:3421`) — the rule-7 collision A64 ruling 2 resolves. `collection_scope` gains a compose-only **three-value `hygiene_tiers`** field: `["operator"]` / `["operator","project"]` / `["operator","project:partial"]`, so an incomplete project scan is scope-distinct from both a complete one and a no-project one; comparability rides the scope axis, not the definition axis, because `promotion_candidate_count`'s meaning is unchanged — only the corpus widens. **Five analyses remain operator-only, unchanged by this slice** — named rather than counted, because the count drifted twice inside this plan's own drafting: `flag_long_instructions`, `check_phantom_refs`, `detect_test_coverage`, `_hooks_body_corpus`, `_staleness_corpus`. T11's real-harness verification (project root `$HOME/src/merkaba`, 604 files, chosen because harness-map's own `.claude/` is effectively unpopulated) measured: real-root suite **1266 passed / 3 skipped**; `phantom_refs` unchanged element-for-element (`check_phantom_refs` was cut from this slice, so nothing in it touches that detector); non-compose output byte-identical to `main` on the DEFAULT profile, proven two ways — narrowed here (F4, post-exec Codex review): under a CUSTOM non-compose profile whose `root_instructions` points at `CLAUDE.local.md` or `.claude/CLAUDE.local.md`, an unreadable file now correctly taints `promotion_candidate_count`/`duplicate_pair_count` where `main` did not, ruled KEEP (the more honest answer) rather than reverted; determinism byte-identical across two `PYTHONHASHSEED` values. `tests/test_collector.py` and `tests/test_render_html.py` **+44 tests, zero deletions** across the branch, now **11 commits** (7 original + 4 review-fix commits). A post-execution Codex review then found six items: four fixed on this branch — **F1** the surface-directory containment gate, **F2** `promotion_candidate_count` reporting partial on an incomplete project hygiene scan, **F3** the version-independent `_probe_is_dir` probe, **F6** the partial-scan documentation seam — one a documentation narrowing already recorded above (**F4**), and one dismissed as a false positive (**F5**, a claimed mock violation — `main` already ships 15 `# mock-ok` annotations and 8 `monkeypatch.setattr` calls for races a real filesystem cannot reproduce, so the new test follows established repo precedent). F1's fix narrows a gap that spec AMENDMENTS A64 had recorded as known-and-not-fixed for **both** `_project_tier_hygiene_corpus` and `_project_tier_duplication_corpus`: the hygiene corpus now gates the surface directory before enumerating it, while the duplication sibling still enumerates before gating and retains the A64 disclosure — an amendment recording this narrowing is **not yet written**. Amendment **A64** merged to the vault (PR #24, merge `59799e0c`). **TRK-026 (duplication-corpus containment) SHIPPED 2026-08-07** (branch `fix/trk-026-duplication-corpus-containment`, 6 commits: `8531bce` the containment gate, `ba200a5` the stale-comment correction, `09f01c6` `_probe_is_dir` + a distinct glob-failure literal, `62596ad` the docstring corrections, `2fcbaeb` the oracle test + key-set pin, `612c5b7` the hygiene-corpus glob-failure distinct-literal fix). Filed as a p3 open DESIGN question — general symlink realpath containment on corpus reads — the investigation found that question already settled for CONTENT: no file bytes can escape the containment root, every project-tier read routing through `_read_project_file`, which binds containment to the opened fd; blanket containment would BREAK the tool, because `~/.claude/skills/harness-map` is itself a symlink to a git checkout, so the scoping is correct by design, not an oversight. **The actual defect, demonstrated with a real temp tree, not reasoned about:** `_project_tier_duplication_corpus` enumerated project surface directories without gating the DIRECTORY through `_project_tier_gate` — only the resulting files were gated. Bytes stayed safe, but the refusal path called `_record_out_of_root_ref` once PER EXTERNAL FILE, serializing absolute external paths into `out_of_root_refs`: measured, a `.claude/rules` symlinked to an external dir holding two `.md` files produced **2** entries with full external paths, while the TRK-023-fixed sibling `_project_tier_hygiene_corpus` on the identical fixture produced **1** entry naming only `.claude/rules`; a third file, `ignored.txt`, never appeared, since the `*.md` filter applied — what makes this a filtered directory listing rather than a one-bit existence oracle. **The fix:** the shape already shipped in the sibling — `_probe_is_dir` → `_project_tier_gate` → enumerate — at three sites, `_probe_is_dir` staying FIRST because `_project_tier_gate` returns `(False, None)` for both "absent" and "escapes" (gating first would misreport every project lacking the directory as an escape, the TRK-050 inverse bug). Verified both directions: 3 escaping dirs with 3 files each yield exactly **3** directory-level entries not 9; four legitimate layouts (absent `.claude`, in-root symlinked rules dir, fully-populated in-root project, both real roots) stay clean at 0 refs / 0 blind spots. **Review: 8 findings raised, 7 fixed, 2 ruled accepted residue.** Fixed: the enumeration leak; a raw `p.is_dir()` where the sibling used `_probe_is_dir` (an `inaccessible != clean` gap on Python 3.14, found independently by three reviewers); a FALSE parity claim the branch's own comment-correction commit introduced (it asserted the two corpus functions "differ only in" one thing — untrue three ways, contradicting a comment 25 lines below it); a comment asserting AMENDMENTS A64 is updated when it is not — the code now states the code fact and names the amendment as outstanding; a duplicated `blind_spots` literal against the TRK-050 T5 F5 precedent, fixed in BOTH siblings (fixing only one would have created a fresh divergence); an untested site-3 per-skill-directory gate, now pinned by an oracle test proven non-vacuous (fails `assert 0 == 1` against pre-fix code); a missing exact-key-set pin. **Accepted residue, ruled NOT in scope:** (a) TOCTOU between the gate and the enumeration — closing it needs fd-based `openat`/`fstat` enumeration, architecture-scale; the sibling ships the identical shape and `_disclose_unlistable_glob`'s docstring already declares this class accepted, not engineered around; (b) `_physical_key` dedup running before the containment gate, so two in-root names symlinking to one external file yield only one `out_of_root_ref` — P3, the external target is still disclosed once, and the identical ordering exists in the sibling, so fixing it only here would re-open the divergence this ticket closed. Also out of scope by operator decision at scoping time: a word/line/token size oracle on a project CLAUDE.md in `walk_always_loaded` (double-gated behind non-compose AND project registration under the operator root, and `_file_entry` has no text field so no content can escape), and `_compose_project_input_paths` (affects only watcher membership). **A65 remains outstanding, and is the operator's to write** (requires a vault merge): AMENDMENTS A64 recorded the directory-level gap as known-and-unfixed for BOTH corpus functions; TRK-023 closed it in the hygiene corpus and TRK-026 has now closed it in the duplication corpus, so A64 understates what is fixed — until A65 exists, the `_project_tier_hygiene_corpus` docstring is the accurate record.
