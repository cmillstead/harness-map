# CLAUDE.md — harness-map Upgrades

You are upgrading the `harness-map` skill per the spec set at the path supplied via the
`HARNESS_MAP_SPEC_DIR` environment variable (referenced below as `SPEC_DIR`; part-files
under `SPEC_DIR/spec/`). The spec set is maintained privately and is not distributed with
this repo. This file is binding.
When code you find conflicts with this file or the spec, the code is wrong — fix it, don't imitate it. **Exception:** where live source contradicts a *fact this spec cites* (a line number moved, a helper renamed, behavior changed by an interim commit), reality wins — record a one-line amendment in `SPEC_DIR/spec/AMENDMENTS.md` per SPEC_1 §4 and proceed per the spec's intent.

## Orientation (read once)

harness-map is a production-grade, read-only inventory of the Claude Code harness: `collector.py` (deterministic stdlib signals → schema-pinned JSON) → model synthesis (judgments → report + synthesis JSON) → `render_html.py` (offline HTML) → `serve.py` (hardened loopback dashboard). It must never write inside the harness it scans, must always emit a valid JSON envelope, and ships portability tests that must never weaken. You are executing a staged upgrade: corrections first (phantom-ref fix, typing, hygiene), then staleness/trends, loop closure, drag economics, and monitoring/generalization. The spec set was verified line-by-line against source on 2026-07-18 (`SPEC_DIR/spec/AMENDMENTS.md`, "Source verification record") — build from it directly; do not re-verify wholesale.

## How to load context (do this, not more)

1. Always: this file + `SPEC_DIR/MASTER_SPEC.md` (index) + `SPEC_DIR/MILESTONE.md` + `SPEC_DIR/spec/AMENDMENTS.md` (skim).
2. Then ONLY the part-files the index's reading list names for your milestone.
3. Update `SPEC_DIR/MILESTONE.md` when an item or milestone completes.

## Binding rules (violations are bugs, not style choices)

1. **Authority order on conflict:** SPEC_1 §2 hard invariants > recorded amendments > this file > MASTER_SPEC conventions > SPEC_2/SPEC_3 > SPEC_4–7 > the audit > existing code style > your judgment.
2. **[DECISION] markers are final.** Do not reopen or work around them. If one blocks you, stop and ask the operator.
3. **Drift protocol.** Source verification is complete; there is no verify-then-build gate. If live source contradicts a spec-cited fact, record a one-line amendment and proceed per the spec's intent; halt only if the contradiction touches a SPEC_1 §2 invariant or makes a [DECISION] unimplementable (SPEC_1 §3). **Symbol names are authoritative; line numbers are advisory.** The tree has drifted far past the 2026-07-18 snapshot (AMENDMENTS A12; current sizes and the command to measure them live under "Key numbers" below — one home, not two); at each milestone start, re-locate every cited symbol by name with Grep — never trust snapshot line numbers.
4. **Read-only posture.** No new write paths, anywhere, ever. The collector writes only a validated outside-root `--out`; the renderer only via `write_html_safely`. `--check` writes nothing.
5. **Envelope rule.** Every new collector field exists (null/empty) in `_empty_document`; `main()` must still emit valid JSON on any crash.
6. **Signals vs judgments.** Collector code never classifies, condemns, or verdicts. Deterministic renderer arithmetic (joins, scores) is a data operation; verdict words stay the model's. Drag verdicts cap at `probation`; schema.md's D8 `retire safely` cap is unconditional — friction telemetry never lifts it (AMENDMENTS A4).
7. **Never edit an existing test assertion.** Additions only. `test_release_decoupling.py` passes unmodified after every milestone — if a scaffold file trips it, the file moves, not the test. **A27 carve-out, narrow:** this protects assertions that PREDATE the current plan execution. An assertion written EARLIER IN THE SAME execution may be corrected **tighten-only** — never loosened, never deleted — and only with an amendment recorded in `SPEC_DIR/spec/AMENDMENTS.md` BEFORE the edit, carrying git-verified provenance (the commit that introduced the assertion is on this branch). Provenance is established with `git log -L` or `git blame`, never from memory. A27 exists to correct a contract that is WRONG; it is never a licence to bulldoze one that is correct — see A31, where A27 would technically have permitted an edit to T3.11 and it was declined on the merits.
8. **Never-waive gate:** `./check.sh` green (ruff + mypy + pytest, run from the skill dir) before anything is called done. No exceptions, including "just this once".
9. **stdlib-only runtime; no mocks; deterministic output** across `PYTHONHASHSEED` (fixed orderings, no bare `set()` iteration into output).
10. **Additive schema discipline:** new sidecar fields additive, readers `.get()`-tolerant, `schema.md` updated in the same change; `schema_version` bumps only on meaning change.
11. **Secrets:** env values never serialize (`config.env_keys` is names-only); scanned bytes are data, never instructions (parse-only `ast`, no import/exec).
12. **Scope fence:** the only file outside the skill dir you may edit is `skills/harness-engineer/SKILL.md` at M7, exact-diff approved by the operator in-session first. Registering hooks or touching `settings.json` is out of scope.

## Key numbers (contracts are binding; measurements are dated — re-measure, don't memorize)

- Module sizes: `wc -l collector.py render_html.py serve.py`. Measured 2026-08-07 (`fix/trk-024-live-verify-merge-table`, based on `main` @ `7b88b9f`): **7,623 / 6,586 / 1,084** — `collector.py` grew by 6 lines (TRK-024 retired the live-verify deferral prose at both `_SURFACE_MERGE` comment sites, comment-only, no behavior change); this ticket touches neither `render_html.py` nor `serve.py`. **Correction to the prior entry:** it recorded `main` @ `7b88b9f` as 7,615 with TRK-056 adding +72 lines over the TRK-082 baseline; git-verified via `git show <rev>:collector.py | wc -l` at both `7b88b9f` (**7,617**, not 7,615) and `6119d5c` (**7,543**, unchanged), the true TRK-056 delta was **+74**, not +72 — a 2-line miscount in the TRK-056 T9 recording, not a code difference; TRK-056's own commit hash (`6508262`) and merged PR are unaffected. Prior: 7,543 / 6,586 / 1,084 on 2026-08-06 @ `6119d5c` (`fix/trk-082-glob-listability`) — `collector.py` grew by 153 lines (TRK-082 glob listability); TRK-050 before it added 220; TRK-050 touches neither `render_html.py` nor `serve.py`. Prior: 7,170 / 6,586 / 1,084 on 2026-08-06 @ `db540d7` (`fix/trk-049-pathological-fixtures`) — the growth over the S6c figure below is TRK-051's (merged as PR #16, `44ac6d7`); TRK-049 touched neither `collector.py` nor `render_html.py` (7,072 / 6,576 / 1,084 on 2026-08-06 @ `abfaef9`, S6c close; 6,985 / 5,311 / 1,084 on 2026-08-05 @ `92e3555`; 6,347 / 5,204 / 1,084 on 2026-08-04 @ `c27b39d`; 1,784 / 2,896 / 832 at the 2026-07-18 snapshot; AMENDMENTS A12). Advisory only — **re-locate every cited symbol by name with Grep; never trust a line number in this file or in the spec.**
- SKILL.md hard cap **200** lines, working cap **195**, budgeted additions **≤26** (SPEC_3 §5) — contracts, changed only by spec amendment. Size: `wc -l SKILL.md`. Measured 2026-08-06: **112** (110 on 2026-08-05; 80 at the 2026-07-18 snapshot, 99 at S1 completion, then 101 both pre-S6a and pre-M11 — `git show 44e1df4^:SKILL.md | wc -l`; 106 pre-M10; AMENDMENTS A12). 112 is inside every cap on either reading of the ≤26 budget's base; if that distinction ever becomes load-bearing, resolve it against SPEC_3 §5, not against this line.
- Description rewrite target **≤70** words (from the verified 88), all 4 positive + 2 negative triggers preserved (SPEC_3 §4).
- Test baseline: `python3 -m pytest -q` from the skill dir. Measured 2026-08-06 (`fix/trk-056-quoted-control-operators` @ `6508262`, macOS): **1229 passed / 4 skipped** in a plain environment; **1233 items** (TRK-056 added 3 tests, all in `tests/test_collector.py`, over the 1226/4/1230 TRK-082 baseline; TRK-082 added 37 tests: 6 in T1, 16 in T2, 11 in T3, 4 in T4, over the 1189/4/1193 TRK-050 baseline; TRK-050 added 22 before it). The 4th skip is still `test_real_root_acceptance_no_crash_envelope_and_hooks_classified`, gated on **`HARNESS_MAP_REAL_ROOT`** (a path to a real harness ROOT DIRECTORY; unset ⇒ skip) — the full-suite figure with the var set is **1230 passed / 3 skipped** (measured 2026-08-06 at `6508262`). `check.sh` still prints a `notice:` line when `HARNESS_MAP_REAL_ROOT` is unset, disclosing that the real-input acceptance check did not run — visible on every plain green run. The `HARNESS_MAP_REAL_SAMPLE` figure was **NOT re-measured at this baseline** — fixture generation remains unavailable — so by the documented 2-test delta (the two smoke tests), re-based onto the new 1229/4 plain baseline, it should read 1231 passed / 2 skipped; treat that as DERIVED, and re-measure before relying on it. Its last measured value was 966 passed / 1 skipped against a 964/3 plain baseline (353 at the 2026-07-18 snapshot; 520 entering S1 and 523 at its completion; 765 at S6a, 851 at S6b, 952 at TRK-041, 960 at TRK-025, 964 at TRK-027+TRK-044, 979 at TRK-047/M10, 981 at the TRK-047 drift pins, 1005 at TRK-051, 1143 at S6c, 1167 at TRK-049, 1189 at TRK-050, 1226 at TRK-082, 1229 at TRK-056 — AMENDMENTS A10, A28, A48, A50, A51–A57). Count only goes up; existing tests untouched.
- **Two measuring traps.** (1) A PIPED `--collect-only` returns a wrapper line reading `Pytest: No tests collected`, which is not the real result — redirect to a file and read the file; a piped plain `python3 -m pytest -q` is unreliable too, producing a spurious `ModuleNotFoundError: No module named 'collector'` that does not occur when redirected to a file — always redirect. (2) `HARNESS_MAP_REAL_SAMPLE` takes a **path to a sidecar named exactly `harness-map-2026-07-15.json`** — not a boolean, and not merely any name matching `find_sidecars`' `harness-map-YYYY-MM-DD.json` regex: both smoke tests render with `--date 2026-07-15`, so the basename must carry that exact date. **Three outcomes; the SKIP case last measured at the TRK-049 baseline (`fix/trk-049-pathological-fixtures` @ `db540d7`), all three now re-based by the documented 2-test delta onto the current 1229/4 plain baseline and treated as DERIVED.** Unset, or a path that does not exist ⇒ both tests **SKIP** (1229 passed / 4 skipped). A file that **exists** under any other name ⇒ both tests **RUN and FAIL** (2 failed / 1229 passed / 2 skipped — derived, see the baseline bullet above) — the skip is gated on `REAL_SAMPLE.is_file()` alone, so an existing-but-misnamed fixture does NOT skip. The error distinguishes the two misses: a basename outside the `harness-map-YYYY-MM-DD.json` regex gives `fatal: zero sidecars found`, whereas a regex-valid but wrong-date name (e.g. `harness-map-2026-07-14.json`) gives `fatal: no sidecar found for date 2026-07-15`. The exact name ⇒ both **RUN and PASS** (1231 passed / 2 skipped — derived, see the baseline bullet above). That middle case is the trap: it is a fixture error, never a renderer regression.
- Test placement (AMENDMENTS A8): renderer features → `test_render_html.py`; collector features → `test_collector.py`. M11 was the single milestone permitted a new test file and has used it — `tests/test_profiles.py` exists; no further new test files.
- Phantom fix: **DONE in S1.M0** — candidate order `root/norm` then `root/<source-dir>/norm`, `_safe_exists` tri-state preserved (SPEC_3 §1; AMENDMENTS A11). Find it by symbol: `check_phantom_refs`, whose two-candidate probe sits just below `src_dir = Path(rel_path).parent`. Line numbers are advisory and have moved twice — `:1166` at snapshot, `:2242` at S1.M0, `:4977` on 2026-08-04.
- Drag: `est_tokens × (1 + friction_events_30d)`, 30-day window computed POST-join, undated records excluded (SPEC_6 §1). Cost: median **events/day** (never "turns/day") over **14** days, min **3** active days (SPEC_6 §2).
- Trend sparklines: display window **N=10** points, appear at ≥3 MEASURED points per series (a crashed run or a missing headline key contributes no point; sidecar count alone is not the gate); loader already exists — visualization only (SPEC_4 §3, AMENDMENTS A2).
- `--check`: exit **0/1/2**; `CHECK_BANDS` = report-template thresholds **5,000 / 12,000** tokens (NOT the renderer's `GAUGE_BANDS` 6,000/15,000 — two homes exist, AMENDMENTS A3); wall-time budget **≤5s** (SPEC_7 §1).
- Kill signals: >2 staleness false positives per real run; >50 mypy errors; >10 type-ignores; 1 edited existing assertion; <10 dated joinable telemetry records at M8 (RISK_REGISTER).
- **When a measured figure above is wrong, correct it in the same change.** `~/.claude/hooks/write-guard.py` (NOT a repo-local hook) is DORMANT while no plan under `docs/plans/` carries `status: in-progress` — with none active the edit is simply allowed. When one IS active it must declare `instruction_files: CLAUDE.md`, or the edit is blocked; routing through the Agent tool alone does NOT satisfy it. `WRITE_GUARD_ALLOW_INSTRUCTION_EDIT=1` does bypass that gate and is the documented recovery for a wedged or ambiguous plan state — but it is an EMERGENCY route only, never the routine one, because it disarms the gate for every instruction file for the whole session.

## Workflow expectations

- `./check.sh` green before claiming any task complete. New behavior needs tests in the same change, real-fixture style (extend `conftest.py::fake_harness` and the `run_collector`/`run_render` subprocess drivers — no mocks).
- Spec constants encoded in tests carry: `# Changing this value requires a spec change (SPEC_N §M).`
- No pre-created stubs: each milestone adds its functions with tests; contracts live in the spec (SPEC_2 §5).
- Fixture policy: fixtures are real temp trees/repos/streams built in-test; nothing golden is regenerated without a spec section saying how.
- No new top-level files in the skill dir beyond the ledger in SPEC_2 §4; no new dependencies, period.
- Commit style: `harness-map S<stage>.M<n>: <imperative summary>`.
- A collector detector change is not done until it has been run once against a real harness root and the expected numbers named in the change. A green suite over `fake_harness` does not certify a detector — the fixture corpus is systematically gentler than the input the tool is built to read. `HARNESS_MAP_REAL_ROOT=<path> python3 -m pytest -q` runs that check.

## Current state

**`SPEC_DIR/MILESTONE.md` is authoritative — read it, and prefer it over this summary, which drifts.** As of 2026-08-06: Stages 1, 2, 6a and 6b have shipped, and the scaffold (`check.sh`, `mypy.ini`) has existed since S1.M0 on 2026-07-24 — source verification completed 2026-07-18. TRK-019 + TRK-020 merged 2026-08-02 (PR #5, `06c6f2a`) and TRK-018 / M11 layout profiles merged 2026-08-04 (PR #6, `c27b39d`) — both dates committer-local per `git log %ci`, and GitHub's UTC `mergedAt` reads one day later for each. **M10 (`--check`, SPEC_7 §1) has SHIPPED** (PR #10, `5ef0572`, plus drift pins in PR #11, `80ef33b`), so **Stage 5 is CLOSED** (AMENDMENTS A49). **TRK-051** then fixed six defects a Codex review found in the shipped gate — F1/F2 (root pre-flight + read-before-write), F3 (profile-rejection envelope unmeasured, both homes), F6 (exit 2 on a non-comparable prior; non-list `civc` guard), F7 (per-metric definition-version skip, AMENDMENTS A50) and F9 (these figures) — with F4, F5 and F8 deferred by explicit operator decision. **TRK-021 (UI polish) SHIPPED 2026-08-05** (PR #13, merge `92e3555`): all five operator findings fixed in `render_html.py` + tests (+11 tests), plus one spec-audit fix (SVG-fallback pin) and one Codex P2 fix (pressed expand-all contrast, now WCAG-pinned against both themes) — so **S7 is CLOSED**. **S6c (TRK-040, trend verdicts, SPEC_6 §6.5a–6.8) SHIPPED 2026-08-06** (`feat/trk-040-s6c-trend-verdicts` @ `abfaef9`, all 14 planned tasks complete): `collection_scope` + per-metric quality state, the derived trend key table, the seven-value verdict enum and classifier, the three-axis (scope/quality/denominator) comparability engine, the `trend_basis` digest recompute with stale-prose suppression, the rendered verdict surface across both trend tables, the A52 blackout disclosure, sparkline presentation and discoverability fixes, plus the `schema.md`/`synthesis-template.json`/SKILL.md updates the deliverable list requires — gate green (1143 passed / 3 skipped), determinism and leak scan clean, **merged as PR #15, merge commit `46376a3`, 2026-08-06** — verified by ancestry (`git branch -r --merged main` lists the branch), not from a memo (AMENDMENTS A51–A57). **TRK-050 slice A+B (comprehension abort) MERGED as PR #18, merge commit `8ea7ae5`, 2026-08-06**: fixed the comprehension-abort defect at **all 12 sites** — measured, one unreadable child aborted the whole generator and lost every sibling while the handler recorded nothing. Split into a listing-level record and a per-child record, with entries sorted before the per-child loop so the record order stays deterministic. Two functions gained an optional `errors` sink following the existing `_skill_has_test_asset(errors=None)` precedent, defaulting to `None` so every existing call site is unchanged. **Category 5 is CLOSED as empty** — both `scandir` sites already recorded correctly. The **20 glob sites are explicitly NOT touched**: measured, `Path.glob()` returns `[]` and raises nothing on a locked directory, so their handlers are dead code for that case — ruled on in spec **AMENDMENTS A59**, carried by **TRK-082**. Verified on the real harness: output is a **no-op vs `main`** — all 10 headline values identical, `errors`/`inaccessible` empty on both, `blind_spots` 9 on both, correct for a defensive fix that speaks only when something is genuinely unreadable. Determinism re-checked: byte-identical across two `PYTHONHASHSEED` values, the only differing field being the wall-clock `generated_at`. Two new tickets filed: **TRK-082** (glob sites) and **TRK-085** (`--check` exits 0 reporting "no baseline" when it could not READ the sidecar directory — needs an exit-code ruling first). A fifth commit then fixed five review findings, the most important being that **absent optional directories were being recorded as collection errors** — the inverse of the bug being fixed, which would have shown 2 problems that do not exist on a healthy harness. Verified in both directions: absent now records 0, present-but-unlistable still records 1, so the fix did not buy silence. The branch shipped **+22 tests with zero test deletions**, and **TRK-086** (wiring the new `errors` sink) is now unblocked because its stated blocker — that absent-directory fix — shipped in this PR. **TRK-082 (glob listability) MERGED as PR #20, merge commit `26ea226`, 2026-08-06** (branch `fix/trk-082-glob-listability`, 5 commits: T1 helper, T2 15 sites, T3 4 pattern-loop sites + a determinism fix, T4 the disclosure) — verified by ancestry (`git log` shows the PR #20 merge commit in this branch's history), not from a memo. The defect: `Path.glob()` returns `[]` and raises nothing on a locked directory, so an unreadable surface read as an empty one — contradicting the skill's own **inaccessible ≠ clean** invariant. Re-measured across all three cases (locked-with-a-real-match, genuinely empty, absent), glob returns `[]` for all three; an `os.scandir` probe separates them. The fix: one helper, `_disclose_unlistable_glob`, probing **only on the empty path**, wired at **19 in-scope sites**. Per **AMENDMENTS A60**, the fixable/disclosed line is a property of the **pattern**, not the call site — a pattern is probeable iff its directory portion carries no wildcard — so the helper self-selects and the 4 pattern-loop sites needed no hand-classification. Over the 18 distinct reachable patterns: **11 probeable, 7 not**. The 7 not-probeable patterns are exactly the `skills/*/…` family and get a **standing, unconditional `blind_spots` entry** rather than a fix, per **AMENDMENTS A59** requirement 4 — the disclosure IS the deliverable for that half. It is unconditional by design: the failure is undetectable at this layer, so a conditional disclosure would be silent precisely when it matters. `_hook_disk_files` was the one site with no sink in scope and gained an optional `errors=None` parameter, the precedent TRK-050 established twice — every existing call site unchanged. `inaccessible` is `list[dict]`, not `list[str]` — the renderer reads `.get("path")` on every entry — so the three sites whose sink is `inaccessible` route through the existing `_append_inaccessible_once` rather than appending a raw string. Verified on the real harness by a same-moment A/B against `main`: **`errors` 0 and `inaccessible` 0 on both**, headline identical, `blind_spots` **9 → 10** (the one new standing entry; every prior entry preserved verbatim and shifted by one). Determinism re-checked: record order byte-identical across four `PYTHONHASHSEED` values after T3's `sorted(set(...))` fix, which was measured producing four different orders before it. The A16 golden literal was regenerated for the new standing entry: structural diff proves **0 entries lost, exactly 1 added, every other key identical**, and both golden assertion functions are **byte-identical**. **TRK-056 (quoted control operators) IN PROGRESS ON BRANCH** `fix/trk-056-quoted-control-operators` @ `8f9c6ca`, 6 commits: T1 (amendment **A62**, committed in the VAULT repo at `bd37f36` — the vault is its own git repo), T2 the quote-aware scanner (`78776f8`), T3 the stale-prose correction (`6508262`), T5 these figures (`b853ca5`), T7 `schema.md`: document the third route into `commands_unparsed` (`f368915`), T8 scope the brace-residual comment to outside-quotes (`8f9c6ca`). The defect: `_has_shell_control_syntax` substring-scanned the RAW command for `$( && || | ; < > { }` and backtick, so an operator sitting **inside quotes** was read as proof a shell was interpreting the command — classifying an opaque external program `no_script` ("fully examined, zero orphan risk") instead of `unparsed` (a disclosed blind spot), inverting the skill's **inaccessible ≠ clean** invariant. The fix: one left-to-right quote-state scan — inside SINGLE quotes nothing counts; inside DOUBLE quotes `$(` and backtick STILL count (they still expand) while the rest do not; a backslash escapes the next character everywhere EXCEPT inside single quotes. A62 **narrows** A45 step 2 — it does not reopen the `[DECISION]`. Deliberately NOT fixed, and recorded as **known residuals of the A45 heuristic** — never "correct", "intentional" or "by design": `&`, newline, `(`/`)`, `${…}`, `$((…))`, bare braces, comment-only and assignment-only input. Rationale is C10 (command-grammar parsing was removed once already after an arms race); narrowing `{`/`}` would additionally break **8 shipped assertions**, which binding rule 7 forbids. Verified on the real harness by a same-moment A/B against `main` with a **positive control** proving the two collectors are genuinely different code (they disagree on `rtk hook "a && z"` — main True, fix False — and agree on the real unquoted `rtk hook claude && echo hi`): **all 7 A/B fields identical** — `hook_commands_examined` 12→12, `hook_commands_total` 12→12, `commands_no_script` 8→8, `commands_unparsed` 0→0, `commands_resolved` 4→4, `commands_total` 12→12, `blind_spots` 10→10 with identical content; `errors` 0 and `inaccessible` 0 on both sides. Zero classification flips is the CORRECT result: every genuine inline-shell command among the 12 real ones carries its operators outside quotes as well. Determinism re-checked: byte-identical across two `PYTHONHASHSEED` values, the only differing field being the wall-clock `generated_at`. `tests/test_collector.py` **+3 tests, zero deletions** — no existing assertion edited. `schema.md` described only 2 of the 3 routes into `commands_unparsed`, omitting the bare-opaque-invocation route (note text `unsupported hook command form: <command>`). That gap PREDATES TRK-056 — verified by direct call, `rtk hook foo` classifies `unparsed` on both `main` and this branch — but A62 routes more commands through it. Verified by running the collector on a purpose-built root: `commands_unparsed` 1 and one `blind_spots` entry reading `unsupported hook command form: rtk hook foo`. The known-residuals comment claimed bare `{`/`}` are scanned "unconditionally, quoted or not". T2 made that false and T8 corrected it. Measured: `rtk hook {foo}` → True, `rtk hook "{foo}"` → False, `rtk hook '{foo}'` → False. Comment only, no behavior change; the residual and its rationale are unchanged. **TRK-024 (merge-table live-verification) IN PROGRESS ON BRANCH** `fix/trk-024-live-verify-merge-table`: the per-surface merge table (`_SURFACE_MERGE`) is live-verified 2026-08-07 against real `claude -p` sessions (CC 2.1.224, macOS) — all six surfaces (skill, command, agent, rule, claude_md, hook) matched the shipped table, including the skill/agent shadow-direction inversion, now measured rather than inferred, not merely documented intent. No code change to the table. Recorded as **AMENDMENTS A63**, merged to the vault repo via PR #23 (`ee818884`). Retired the stale `live-verify deferred 2026-07-22` deferral prose at both `collector.py` `_SURFACE_MERGE` comment sites and at `SKILL.md`'s compose-mode paragraph; comment-only, no behavior change.
